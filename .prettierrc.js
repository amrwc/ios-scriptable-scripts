/**
 * Prettier configuration. Place it in the parent directory of the relevant
 * files, or in the root of a project if the configuration should be applied
 * everywhere.
 *
 * @author amrwc
 * @read https://prettier.io/docs/en/options.html
 * @read https://prettier.io/docs/en/configuration.html
 */
module.exports = {
	arrowParens: 'avoid',
	bracketSpacing: true,
	printWidth: 100,
	quoteProps: 'as-needed',
	semi: true,
	singleQuote: true,
	trailingComma: 'es5',
	tabWidth: 4,
	useTabs: true,
};
